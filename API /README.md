It fetches raw data from the external API (REST, auth optional).
Normalises and cleans it.
Sends part of the data to an LLM with a structured prompt.
Gets back a summary, classification, tags, or recommendations.
Returns a combined JSON object.